export type Momento = {
  id: string;
  icono: string;
  titulo: string;
  descripcion: string;
  fecha: string;
  fecha_orden: string | null;
  imagen_url: string | null;
  orden: number;
  created_at: string;
};